apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: domain-backend
  namespace: dev
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-ssm
    kind: ClusterSecretStore
  target:
    name: domain-backend
  dataFrom:
    - find:
        name:
          regexp: "^/dev/domain-backend/"
      rewrite:
        - regexp:
            source: "^/dev/domain-backend/(.*)"
            target: "$1"
