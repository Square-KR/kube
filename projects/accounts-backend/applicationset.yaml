apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: accounts-backend
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: projects
    app.kubernetes.io/component: user-service
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]

  generators:
    - list:
        elements:
          - env: dev

  template:
    metadata:
      name: accounts-backend-{{.env}}
      labels:
        app.kubernetes.io/part-of: projects
        app.kubernetes.io/component: user-service
        app.kubernetes.io/instance: "{{.env}}"
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default

      sources:
        - repoURL: https://github.com/Square-KR/kube
          targetRevision: HEAD
          ref: repo

        - repoURL: https://github.com/Square-KR/kube
          targetRevision: HEAD
          path: charts/app
          helm:
            releaseName: accounts-backend
            valueFiles:
              - $repo/projects/accounts-backend/{{.env}}/values.yaml

        - repoURL: https://github.com/Square-KR/kube
          targetRevision: HEAD
          path: projects/accounts-backend/{{.env}}
          directory:
            exclude: values.yaml

      destination:
        server: https://kubernetes.default.svc
        namespace: "{{.env}}"

      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - ServerSideApply=true
          - CreateNamespace=true
